{
  "devDependencies": {
    "globals": "^16.0.0",
    "husky": "^8.0.0",
    "lint-staged": "^15.4.3",
    "prettier": "^3.5.3"
  },
  "scripts": {
    "prepare": "husky && husky install",
    "lint": "eslint ."
  },
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx,json,css,md}": "prettier --write"
  }
}
