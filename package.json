{
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^15.4.3",
    "prettier": "^3.5.3"
  },
  "scripts": {
    "prepare": "husky init"
  },
  "lint-staged": {
    "*.ts": "prettier --write",
    "*.js": "prettier --write",
    "*.css": "prettier --write",
    "*.json": "prettier --write",
    "*.md": "prettier --write"
  }
}
