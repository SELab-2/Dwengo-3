{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3",
    "prettier": "^3.5.3"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.ts": "prettier --write",
    "*.js": "prettier --write",
    "*.css": "prettier --write",
    "*.json": "prettier --write",
    "*.md": "prettier --write"
  }
}
