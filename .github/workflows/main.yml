name: Deploy on push to main

on:
  push:
    branches:
      - main

run-name: ${{ github.actor }} has pushed an update to ${{ github.ref }} branch 🚀

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies for all projects
        run: ./install.sh

      - name: Generate the client and migrate the database
        run: ./prisma.sh migrate deploy

      - name: Build client
        run: cd client && npm run build

      - name: Restart applications
        run: pm2 restart client server
